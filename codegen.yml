generates:
  ./src/client/types/graphql-schema-types.ts:
    schema: "src/server/graphql/schema.graphql"
    documents: "src/client/**/*.{ts,tsx,gql,graphql}"
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
